const stripe=require('stripe')(process.env.STRIPE_SECRET_KEY),router=require('express').Router();router.post('/create-migration-session',async(req,res)=>{const{tier,siteUrl,email}=req.body,prices={standard:'price_standard_49',express:'price_express_99',whiteglove:'price_white_299'};try{const session=await stripe.checkout.sessions.create({payment_method_types:['card'],line_items:[{price:prices[tier],quantity:1}],mode:'payment',success_url:`${process.env.DOMAIN}/migration/success?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${process.env.DOMAIN}/migration/cancel`,customer_email:email,metadata:{siteUrl,tier,service:'migration'}});res.json({sessionId:session.id,url:session.url})}catch(e){res.status(500).json({error:e.message})}});router.post('/create-venice-subscription',async(req,res)=>{const{email,userId}=req.body;try{const customer=await stripe.customers.create({email}),subscription=await stripe.subscriptions.create({customer:customer.id,items:[{price:'price_venice_29_monthly'}],payment_behavior:'default_incomplete',expand:['latest_invoice.payment_intent'],metadata:{userId}});res.json({subscriptionId:subscription.id,clientSecret:subscription.latest_invoice.payment_intent.client_secret})}catch(e){res.status(500).json({error:e.message})}});router.post('/webhook',require('express').raw({type:'application/json'}),(req,res)=>{const sig=req.headers['stripe-signature'],event=stripe.webhooks.constructEvent(req.body,sig,process.env.STRIPE_WEBHOOK_SECRET);if(event.type==='checkout.session.completed')console.log('Migration:',event.data.object.metadata);else if(event.type==='invoice.payment_succeeded')console.log('Venice sub:',event.data.object.metadata.userId);res.json({received:true})});module.exports=router;
